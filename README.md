If you want to become an admin on the Inspiration School website (PPDB), you can create an email (when registering) with nama@admin.library, it will automatically become an admin (role changes to admin).
